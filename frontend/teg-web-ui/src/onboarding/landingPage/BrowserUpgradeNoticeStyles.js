import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  root: {
  },
  logo: {
    maxWidth: '80%',
  },
  buttonLink: {
    textDecoration: 'none',
  },
  button: {
    marginTop: 15,
    marginBottom: 20,
  },
}))

export default useStyles
