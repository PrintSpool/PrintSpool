name: Rust

on:
  push:
    branches: [ main, develop ]
#   pull_request:
#     branches: [ develop ]

env:
  CARGO_TERM_COLOR: always
  SQLX_OFFLINE: "true"

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v2
    - name: Install Dependencies
      run: |
        sudo apt-get install gcc-multilib
        sudo apt-get remove gcc-multilib
        sudo apt-get install \
          g++-arm-linux-gnueabihf
        rustup target add armv7-unknown-linux-gnueabihf
    - name: Build Armv7
      run: |
        cd crates/server && cargo build --target=armv7-unknown-linux-gnueabihf
#     - name: Run tests
#       run: cargo test --verbose

# libc6-dev-armhf-cross \
# cargo install sqlx-cli

# pushd .
# echo sqlite:`pwd`/db.sqlite
# cd ./crates/machine
# DATABASE_URL=sqlite:`pwd`/db.sqlite sqlx db setup
# popd
# DATABASE_URL=sqlite:`pwd`/db.sqlite
# echo sqlite:`pwd`/db.sqlite
# echo "database_url: $DATABASE_URL"
# DATABASE_URL=sqlite:`pwd`/db.sqlite cargo build --target=armv7-unknown-linux-gnueabihf
