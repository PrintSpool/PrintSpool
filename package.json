{
  "private": true,
  "scripts": {
    "postinstall": "npx babel-node ./scripts/postinstall.js",
    "dev": "npx lerna run dev --parallel",
    "test": "cd ./packages/tegh-daemon/ && yarn test && cd ../tegh-driver-serial-gcode && yarn test",
    "build": "npx lerna run build --parallel",
    "start": "npx lerna run start --parallel",
    "cloc": "cloc . --exclude-dir=node_modules,dist,.next,log"
  },
  "babel": {
    "presets": [
      "env"
    ],
    "plugins": [
      "transform-object-rest-spread"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-eslint": "^8.0.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "lerna": "^2.5.1",
    "bluebird": "^3.5.1"
  }
}
