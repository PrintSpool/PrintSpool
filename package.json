{
  "name": "teg",
  "version": "0.13.1",
  "repository": "git@github.com:tegapp/teg.git",
  "author": "D1plo1d",
  "license": "AGPL-3.0",
  "standard-version": {
    "scripts": {
      "postchangelog": "VERSION=`node -p \"require('./package.json').version\"` replace 'version: .*' 'version: $VERSION' ./snapcraft/*/snapcraft.yaml"
    }
  },
  "devDependencies": {
    "replace": "^1.2.1",
    "standard-version": "^9.2.0"
  }
}
