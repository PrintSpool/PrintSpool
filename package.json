{
  "private": true,
  "scripts": {
    "bootstrap": "yarn && npx lerna clean --yes && cd ./packages/tegh-server && yarn && yarn build && cd ../../ && npx lerna bootstrap --concurrency 1",
    "dev": "npx lerna run dev --parallel",
    "test": "cd ./packages/tegh-server/ && yarn test && cd ../tegh-driver-serial-gcode && yarn test",
    "build": "npx lerna run build --parallel",
    "start": "npx lerna run start --parallel",
    "cloc": "cloc . --exclude-dir=node_modules,dist,.next,log"
  },
  "command": {
    "bootstrap": {
      "npmClient": "yarn"
    }
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
